[playbook]
dir = ./

[group_vars]
dir = inventories/group_vars

[env]
env = production,staging,development,test

[list_option]
options = hosts,tags,tasks

[tags_option]
options = nginx-reload,nginx-restart,db-schema,db-user

